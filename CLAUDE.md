# learned_traj_compress

We present MemDistill, a framework for understanding and overcoming the catastrophic SFT collapse when distilling RL-trained agent memory policies into tiny (<3B) sidecar models. While 'SFT Memorizes,
